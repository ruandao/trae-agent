---
description: onlineService 入站/出站 HTTP 请求日志约定，避免漏记
alwaysApply: true
---

# onlineService HTTP 请求日志（元规则）

在 `onlineService/` 内改动或新增网络相关代码时，**必须**维持下列约定，避免入站或出站请求漏记。

## 入站（客户端 → 本服务）

- **位置**：`onlineService/logs/`，主文件 `requests.log`（按行滚动，见 `RotatingFileHandler`）。
- **实现**：`app/main.py` 中 `_RequestAccessLogMiddleware` + `paths.logs_dir()`；**不要**移除或绕过该中间件另起 ASGI 入口而不打访问日志。
- **本地开发**：`run_local.sh` 已 `mkdir` 且 `--reload-exclude` 包含 `logs/`；若新增同类持久化目录，应同样排除以免触发热重载。

## 出站（本服务 → 外部 HTTP）

- **位置**：`onlineService/reqLogs/`，主文件 `outbound.log`。
- **当前统一出口**：`app/task_api_bootstrap.py` 的 `_post_json`（内部调用 `_log_outbound_post`、脱敏、文件 handler）。
- **新增出站请求时**：
  - **优先**扩展 `_post_json` 或抽出的共享模块（与现有 `_redact_for_log` / `_ensure_outbound_req_log_handler` 同套路），保证每条对外请求都写入 `reqLogs`；
  - **禁止**在 `onlineService/app/` 中新增裸 `urllib.request.urlopen`、`httpx`、`requests` 等对外调用且**不**经上述日志与脱敏路径（除非先补齐等价日志与脱敏后再用）。
- **脱敏**：请求体/错误详情落盘前须脱敏：`access_token`、`refresh_token`、以 `_token` / `_secret` 结尾的键名等敏感字段用占位符（如 `***`），与 `_redact_for_log` 语义一致。
- **本地开发**：`run_local.sh` 已 `mkdir` 且 `--reload-exclude` `reqLogs/`；Dockerfile 已预建对应目录。

## 维护提示

若将出站日志逻辑抽到独立模块，本规则中的文件名（`_post_json` 等）应同步更新描述，但**目录与「凡出站必记、必脱敏」要求不变**。
